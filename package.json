{
  "name": "reyu-diamond-app",
  "private": true,
  "scripts": {
    "client": "npm start --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "npm run server"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "node-cron": "^4.2.1"
  }
}
